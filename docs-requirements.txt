# Documentation-only dependency surface for MkDocs CI and GitHub Pages.
# The broader local development environment also includes this package via
# requirements.txt so existing full-dev installs can still run `make docs-build`.
mkdocs-material>=9.6,<10.0
pyyaml>=6.0,<7.0
cairosvg>=2.7,<3.0
